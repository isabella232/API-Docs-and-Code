var unirest = require("unirest");

var req = unirest("GET", "http://preprod.api.datatiger.com/datatypes");

req.headers({
  "content-type": "application/json"
});


req.end(function (res) {
  if (res.error) throw new Error(res.error);

  console.log(res.body);
});
